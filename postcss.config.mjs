/**
 * Konfigurasi PostCSS Karsa.
 * Tailwind CSS v3 → pakai plugin `tailwindcss` + `autoprefixer`.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
